--!nonstrict
-- ai-menu control-center quick tile: opens the AI Menu panel.
-- Thin client: no logic, just toggles the panel (noctalia.togglePanel).

function onClick()
  noctalia.togglePanel("tranzem/ai-menu:panel")
end
